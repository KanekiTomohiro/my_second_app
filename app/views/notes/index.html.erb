<h1>投稿の一覧</h1>
<ul class="notes">
  <!-- eachメソッドで@notesに入ったものを一つずつ取り出している -->
  <% @notes.each do |note| %>
   <li>
    <span class="content">
      <%= link_to note.title, note_path(note.id) %>
    </span>
   </li>
  <% end %>
  
  <%= paginate(@notes) %>
  
</ul>

