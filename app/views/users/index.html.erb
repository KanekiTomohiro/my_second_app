<h1 class="heading">ユーザ一覧</h1>


<ul class="user-list">
  <% @users.each do |user| %>
    <li>
      <!-- imageがあればそれを、なければデフォルトを表示させるメソッドの呼び出し -->
      <%= image_for(user) %>
      <%= link_to user.name, user %> |
      <%= user.email %>
    </li>
  <% end %>
</ul>